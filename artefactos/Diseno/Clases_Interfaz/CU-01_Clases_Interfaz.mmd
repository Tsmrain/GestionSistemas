classDiagram
    class IDisponibilidad {
        <<interface>>
        +consultarDisponibilidad(tipo, fecha)
        +validarFecha(fecha)
    }

    class IHabitacion {
        <<interface>>
        +obtenerHabitacionesDisponibles(tipo, fecha)
        +estaDisponible()
    }

    class IVistaBusqueda {
        <<interface>>
        +mostrarFormulario()
        +mostrarResultados(lista)
        +mostrarError(mensaje)
    }

    class DisponibilidadController {
        +consultarDisponibilidad(tipo, fecha)
        +validarFecha(fecha)
    }

    class Habitacion {
        +numero
        +tipo
        +estado
        +precio
        +obtenerHabitacionesDisponibles(tipo, fecha)
        +estaDisponible()
    }

    class PaginaBusqueda {
        +mostrarFormulario()
        +mostrarResultados(lista)
        +mostrarError(mensaje)
    }

    IDisponibilidad <|.. DisponibilidadController
    IHabitacion <|.. Habitacion
    IVistaBusqueda <|.. PaginaBusqueda
    DisponibilidadController --> Habitacion : consulta
    PaginaBusqueda --> DisponibilidadController : solicita